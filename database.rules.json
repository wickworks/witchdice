{
  /* Visit https://firebase.google.com/docs/database/security to learn more about security rules. */
  "rules": {
    "rolls": {
      "$room": {
        ".read": true,
        ".write": true,
        ".validate": "$room.length > 6 && $room.length < 40 && $room.matches(/[^A-Za-z]/i)"
      }
    },

    "initiative": {
      "$room": {
        ".read": true,
        ".write": true,
        ".validate": "$room.length > 6 && $room.length < 40 && $room.matches(/[^A-Za-z]/i)"
      }
    },

    // but no other child paths are allowed
    "$other": {
    	".read": false,
    	".write": false,
    }
  }
}
